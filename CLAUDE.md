# 이 저장소에서 일하는 규칙

## 저장소는 여기 하나입니다

`lulus-cat/peropix-mobile` 만 작업합니다.

`lulus-cat/peropix-Lkit-mobile` 은 **손대지 마세요.** 개인용 비공개 저장소이고,
예전에는 `www/` 를 그쪽에도 똑같이 복사해 두었지만 지금은 그러지 않습니다.
브랜치를 밀지도, 병합하지도, 빌드를 걸지도 않습니다. 비공개 설정도 그대로 둡니다.

## 어디에 무엇이 있나

화면과 로직은 전부 `www/` 안의 HTML·JS 입니다. Capacitor 로 감싼 하이브리드 앱이라
`www/` 에는 빌드 단계가 없습니다. 고치면 바로 반영됩니다.

`android/` 는 네이티브 쪽입니다. 여기를 고치면 APK 를 새로 만들어야 적용됩니다.
매니페스트 권한을 바꾼 경우 특히 그렇습니다.

## 검사

`npm test` 가 단위 검사를 전부 돌립니다. 파이썬 수신함은 `python3 tools/test_receiver.py`.
**검사를 통과 못 하면 APK 를 안 만듭니다.**

로직을 고쳤으면 검사를 같이 고치거나 늘립니다. 특히 이런 것들은 검사가 없으면
조용히 깨집니다.

- 화면 뼈대 (`tools/test_html.js`) — 태그 짝이 안 맞아도 브라우저는 오류를 안 냅니다.
  알아서 고쳐 그리는데 그 모양이 짠 것과 다를 뿐입니다.
- 셸에 넘기는 글자 (`tools/test_ssh.js`) — 따옴표가 어긋나면 남의 서버에서 엉뚱한
  명령이 돕니다.
- NAI 페이로드 (`tools/verify_payload.js`) — 틀리면 돈만 쓰고 다른 그림이 나옵니다.

## 배포

`main` 에 푸시하면 GitHub Actions 가 APK 를 만들어 Releases 에 붙입니다.
브랜치에서는 Actions 탭에서 손으로 돌려야 합니다.

판 번호는 이미 붙은 태그 중 제일 높은 것에서 하나 올립니다. 실행 횟수로 매기지 마세요.

## 지켜야 할 선

- **NovelAI 키는 폰에만 있습니다.** 수신함이나 저장소로는 「무엇을 뽑을지」 만
  올라갑니다. NAI 를 부르는 것도 Anlas 를 쓰는 것도 폰입니다.
- **앱은 사용자의 저장소에 쓰지 않습니다.** 쓰기 권한이 있는 토큰을 폰에 두지 않습니다.
- **업데이트 확인에 사용자의 토큰을 붙이지 않습니다.** 공개 저장소를 보는 일이고,
  남의 한도를 대신 쓸 이유가 없습니다.

## 글

README 와 릴리스 노트는 `/humanizer` 를 거쳐 씁니다.
화면에 나가는 글도 같은 기준입니다. 번역체를 피하고, 외래어와 한글로 된 외국어는
그대로 씁니다. 결과를 말해 주는 문장을 씁니다 — 「키를 삭제하면 첫 실행 화면이
나옵니다」 보다 「키를 지워도 슬롯·인물·서랍은 그대로 남습니다」 쪽입니다.

화면 글은 `tools/test_korean.js` 가 지킵니다 (`npm test` 에 들어 있습니다).
조사·맞춤법·띄어쓰기·번역투·용어를 봅니다. 사람이 보는 글만 보고 주석은 안 봅니다.

정해 둔 말이 있습니다. **폰**(기기 아님), **앱**, **설정**(세팅 아님), **받기**
(다운로드 아님), **저장소**. 보조용언은 띄어 씁니다 — 「넣어 주세요」, 「해 보세요」.
느낌표는 안 씁니다.

말을 한꺼번에 바꿀 때 조사가 안 따라오는 일이 잦습니다. 「그림」 을 「이미지」 로
바꾼 자리에 「이미지을」 이 남아 있었습니다. 바꾼 뒤 검사를 꼭 돌리세요.
